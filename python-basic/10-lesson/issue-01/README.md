# Кодирование строки в азбуку Морзе

Функция `encode` кодирует строку в соответствии с таблицей азбуки Морзе.

## Запуск тестов

Для запуска тестов используйте команду:
```linux
python -m doctest -o NORMALIZE_WHITESPACE -v morse.py
```